<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/spacing_s"
    android:layout_marginVertical="@dimen/spacing_xs"
    app:cardBackgroundColor="@color/background_main"
    app:cardCornerRadius="@dimen/radius_m"
    app:cardElevation="2dp"
    app:cardPreventCornerOverlap="false"
    app:strokeColor="@color/background_sub"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/spacing_l">

        <!-- Header Row -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                android:id="@+id/ivCheckoutOptionIcon"
                android:layout_width="@dimen/icon_size"
                android:layout_height="@dimen/icon_size"
                android:layout_alignParentStart="true"
                android:layout_centerVertical="true"
                android:contentDescription="@null"
                app:tint="@color/button"
                tools:src="@drawable/ic_location" />

            <TextView
                android:id="@+id/tvCheckoutOptionTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerVertical="true"
                android:layout_marginStart="@dimen/spacing_s"
                android:layout_toEndOf="@id/ivCheckoutOptionIcon"
                android:layout_toStartOf="@+id/tvCheckoutOptionEdit"
                android:ellipsize="end"
                android:fontFamily="@font/nunito_bold"
                android:maxLines="1"
                android:textColor="@color/text_main"
                android:textSize="14sp"
                tools:text="Địa chỉ nhận hàng" />

            <TextView
                android:id="@+id/tvCheckoutOptionEdit"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:fontFamily="@font/nunito_semibold"
                android:padding="@dimen/spacing_xs"
                android:text="@string/checkout_edit"
                android:textColor="@color/button"
                android:textSize="13sp" />
        </RelativeLayout>

        <!-- Content Area -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_s">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_alignParentStart="true"
                android:layout_toStartOf="@+id/tvCheckoutOptionRightValue"
                android:orientation="vertical"
                android:layout_marginStart="@dimen/checkout_content_indent">

                <TextView
                    android:id="@+id/tvCheckoutOptionPrimary"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:fontFamily="@font/nunito_semibold"
                    android:textColor="@color/text_main"
                    android:textSize="14sp"
                    tools:text="Nguyễn Văn A | 0987654321" />

                <TextView
                    android:id="@+id/tvCheckoutOptionSecondary"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/spacing_xs"
                    android:fontFamily="@font/nunito_regular"
                    android:textColor="@color/text_secondary"
                    android:textSize="13sp"
                    tools:text="123 Đường ABC, Phường 4, Quận 5, TP. Hồ Chí Minh" />
            </LinearLayout>

            <TextView
                android:id="@+id/tvCheckoutOptionRightValue"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:fontFamily="@font/nunito_bold"
                android:textColor="@color/accent_dark"
                android:textSize="14sp"
                tools:text="30.000đ" />
        </RelativeLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
