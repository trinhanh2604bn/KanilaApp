<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/spacing_m"
    android:background="@drawable/bg_card"
    android:padding="@dimen/spacing_m">

    <ImageView
        android:id="@+id/ivTaskIcon"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:background="@drawable/bg_icon_soft"
        android:contentDescription="@null"
        android:padding="6dp"
        android:src="@drawable/ic_calendar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@color/button" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/spacing_m"
        android:layout_marginEnd="@dimen/spacing_m"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/ivTaskStatus"
        app:layout_constraintStart_toEndOf="@id/ivTaskIcon"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/tvTaskTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/nunito_bold"
            android:textColor="@color/text_main"
            android:textSize="14sp"
            tools:text="Làm sạch da buổi sáng" />

        <TextView
            android:id="@+id/tvTaskSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/nunito_regular"
            android:textColor="@color/text_secondary"
            android:textSize="12sp"
            tools:text="Cập nhật mỗi ngày để nhận thưởng" />
    </LinearLayout>

    <ImageView
        android:id="@+id/ivTaskStatus"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:contentDescription="@null"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/ic_chevron_right" />

    <!-- Hidden checkbox to keep binding working if necessary -->
    <com.google.android.material.checkbox.MaterialCheckBox
        android:id="@+id/cbTask"
        android:layout_width="1dp"
        android:layout_height="1dp"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
