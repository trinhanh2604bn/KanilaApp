<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layoutShippingMethodRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/spacing_l"
    android:layout_marginVertical="@dimen/spacing_s"
    android:background="@drawable/bg_address_card"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:padding="@dimen/spacing_l">

    <ImageView
        android:id="@+id/ivShippingMethodRadio"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:contentDescription="@null"
        android:src="@drawable/selector_address_radio" />

    <FrameLayout
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginStart="@dimen/spacing_m"
        android:background="@drawable/bg_shipping_icon_container">

        <ImageView
            android:id="@+id/ivShippingMethodIcon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:contentDescription="@null"
            android:src="@drawable/ic_delivery_truck"
            app:tint="@color/button" />
    </FrameLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/spacing_m"
        android:layout_weight="1"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tvShippingMethodName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/nunito_bold"
                android:textColor="@color/text_main"
                android:textSize="15sp"
                tools:text="Tiêu chuẩn" />

            <TextView
                android:id="@+id/tvShippingMethodTag"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/spacing_s"
                android:background="@drawable/bg_status_return"
                android:fontFamily="@font/nunito_medium"
                android:paddingHorizontal="@dimen/spacing_s"
                android:paddingVertical="2dp"
                android:text="Phổ biến"
                android:textColor="@color/status_return_text"
                android:textSize="10sp"
                android:visibility="gone"
                tools:visibility="visible" />
        </LinearLayout>

        <TextView
            android:id="@+id/tvShippingMethodEstimate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:fontFamily="@font/nunito_regular"
            android:textColor="@color/text_main"
            android:textSize="13sp"
            tools:text="Dự kiến giao: 2 - 3 ngày" />

        <TextView
            android:id="@+id/tvShippingMethodDescription"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="@font/nunito_regular"
            android:maxLines="2"
            android:textColor="@color/text_tertiary"
            android:textSize="12sp"
            tools:text="Giao hàng tiêu chuẩn, phù hợp mọi đơn hàng" />
    </LinearLayout>

    <TextView
        android:id="@+id/tvShippingMethodPrice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:layout_marginStart="@dimen/spacing_s"
        android:fontFamily="@font/nunito_bold"
        android:textColor="@color/text_main"
        android:textSize="15sp"
        tools:text="15.000đ" />

</LinearLayout>
